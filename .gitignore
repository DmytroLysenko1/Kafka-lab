plan.md
status.local.md
notes.local.md

.idea/
.vscode/
*.iml

/bin/
/dist/
# `go build ./path/to/cmd` drops the binary in the working directory under the package
# directory's name; the Makefile never builds one, so anything matching is a stray.
/exp-*-*
*.test
/coverage.out
*.tmp.out


.env
.env.local

/experiments/tmp/
# Experiment results are the evidence this repository exists for and are committed;
# only scratch logs are ignored.
*.tmp.log

/docs/dynamic/dist/

.DS_Store
